import 'package:jaspr/dom.dart';

// All visual styles are declared in web/styles.css (served as a static asset).
// This file only registers the empty @css list so jaspr_builder is satisfied.

@css
List<StyleRule> get styles => [];
